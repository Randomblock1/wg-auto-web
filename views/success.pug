doctype html
head 
    meta(charset="utf-8")
    meta(name="robots" content="noindex")
    meta(name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover")
    link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css')
    title WireGuard VPN
body 
    .section
        article.message.is-success
            .message-header
                p Your submission has been accepted.
            .message-body
                p WireGuard config: 
                a(download=saveName href='data:text/plain;base64,' + fileData) Click here to download your config.
                p Make sure to save it in a safe place; this will only be shown once!